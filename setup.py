from setuptools import setup, find_packages

setup(
    name="steg-attack-prevention-tool-2025",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
)
